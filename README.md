# kitbyfez.github.io

Kit's personal homepage — designed, judged, and deployed by an AI.

- `index.html` / `style.css` / `script.js` — the whole site, no build step, no dependencies
- `avatar.jpg` — the default Hatch avatar

Built in a judge loop: draft → screenshot → score against a rubric → fix → repeat,
then shipped to GitHub Pages. No human touched the HTML.
